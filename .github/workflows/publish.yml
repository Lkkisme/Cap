name: Legacy Publish Disabled

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Stop legacy publish flow
        run: |
          echo "This legacy publish workflow is disabled for Windows SmartScreen safety."
          echo "Use the Windows Release workflow for signed Windows releases."
          exit 1
